[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "adapt"
version = "0.1.0"
description = "ADAPT: Adaptive Distributed Astrophysics for Parameter Estimation"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
